<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             android:background="@color/colorPrimary"
             android:layout_height="@dimen/material_48dp"
             android:paddingTop="@dimen/material_8dp"
             android:paddingBottom="@dimen/material_8dp"
              android:layout_width="match_parent">

    <TextView
        android:id="@+id/view_loading_button_text"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_gravity="center"
        android:background="@color/colorPrimary"
        android:text="@string/login.btn.text"
        android:textColor="@android:color/white"
        android:gravity="center"
        android:textSize="18sp"/>

    <ProgressBar
        android:id="@+id/view_loading_button_progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:visibility="gone"/>
</FrameLayout>